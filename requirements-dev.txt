# Third-party runtime dependencies needed to collect and run the full
# services/*/tests suite locally (beyond the editable installs already covered
# by INSTALLATION.md, section "Development machine"):
#
#   python3 -m venv .venv
#   .venv/bin/pip install -e libs/energy_node_common -e libs/battery_soc_core pytest
#   .venv/bin/pip install -r requirements-dev.txt
#
# These match the node-side runtime deps in INSTALLATION.md section 3.2 —
# without them, pytest fails to even collect the apsystems_ez1, shelly,
# trucki and tuya_mqtt test modules (ModuleNotFoundError).
apsystems-ez1
tinytuya
requests

# Only for regenerating the Home Assistant icon module
# (scripts/icons/flatten_icons.py). Not needed for any test: its --check mode
# and integrations/homeassistant/tests/test_icon_set.py are stdlib-only.
# shapely 2.0.x has no Python 3.14 wheels, hence >=2.1.
svgelements==1.9.6
shapely>=2.1
