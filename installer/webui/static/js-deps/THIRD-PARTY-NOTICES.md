# Third-party assets in this directory

| File | Project | License |
|---|---|---|
| `alpine.min.js` | Alpine.js | MIT |

The same build the dashboard embeds (`dashboard/internal/webui/static/js-deps/`).
Keep both in step: from Plan D on they end up in the same binary.
